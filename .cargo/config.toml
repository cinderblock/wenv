# no_std / no-CRT build: we link only OS DLLs (kernel32, shell32 on Windows) and
# the system libc on Unix, so there is no C runtime to statically link. The
# Windows entry point and subsystem are passed via build.rs (robust against
# tooling that sets RUSTFLAGS); the allocator and intrinsics live in src/.
